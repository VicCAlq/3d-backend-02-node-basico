{
  "name": "Atividade",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "node ./src/app.js",
    "atv1": "node ./src/AtvEnviarHTML.js",
    "atv2": "node ./src/AtvEnviarArquivo.js",
    "atv3": "node ./src/AtvEnviarJson.js",
    "atv4": "node ./src/AtvTresRotas.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --reporter=json --output-file=./resultado.json",
    "tr": "timeout --signal=SIGKILL 5s npx vitest run --reporter=json --output-file=./resultado.json --pool=forks --testTimeout=5000 || true",
    "th": "vitest --help",
    "trh": "vitest run --help"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "express": "^5.2.1",
    "sqlite": "^5.1.1"
  }
}
